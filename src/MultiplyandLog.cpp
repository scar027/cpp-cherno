#include <iostream>
#include "sample-library/MultiplyandLog.h"

void MultiplyandLog(int a, int b)
{
    int result = a * b;
    std::cout << "The result of the multiplication of " << a << " & " << b << " is: " << result << std::endl;
}